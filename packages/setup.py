"""Used to install all the packages in this folder and its subfolders."""
from setuptools import setup, find_packages
setup(
    name="dataset_manipulation",
    version="0.1",
    packages=find_packages(),
)
